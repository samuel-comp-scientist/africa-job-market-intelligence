import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "African Job Market Intelligence Platform",
  description: "Discover tech job trends, skill demands, salary insights, and future predictions across Africa",
  keywords: "African jobs, tech careers, job market analysis, skill trends, salary insights",
  authors: [{ name: "African Job Market Intelligence Team" }],
  openGraph: {
    title: "African Job Market Intelligence Platform",
    description: "Data-driven insights for African tech job market",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
